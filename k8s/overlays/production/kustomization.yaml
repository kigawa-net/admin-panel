apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../../base

namespace: admin-panel

images:
  - name: ghcr.io/kigawa-net/admin-panel
    newTag: latest

patches:
  - path: replica-patch.yaml
  - path: ingress-tls-patch.yaml
