apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: admin-panel
spec:
  ingressClassName: haproxy
  rules:
    - host: admin.kigawa.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: admin-panel
                port:
                  number: 80
